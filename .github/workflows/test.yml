# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: TEST
'on': push
jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - name: Extract branch data
        shell: bash
        run: echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
        id: extract_branch_data

  cypress-run:
    needs: test1
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Cypress run
        uses: cypress-io/github-action@v1
        with:
          spec: cypress/integration/test3/*
          env: true
        env:
          CYPRESS_BASE_URL: 'https://clilco-webrtc-tests.web.app/'



#https://github.blog/changelog/2020-10-01-github-actions-deprecating-set-env-and-add-path-commands/ ---read this